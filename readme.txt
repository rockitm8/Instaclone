1. Before running node js, redis server and mongodb should be installed
2. The port of mongodb while installing should be set to 27017 and of redis server to 6379
3. After installing the above packages unzip the zipped folder and locate it through command prompt
3. Run the command 'node instaserver.js'
4. Open the browser and go to 'localhost:3000' 

